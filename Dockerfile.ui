FROM ubuntu:latest

# https://pythonspeed.com/articles/activate-conda-dockerfile/

WORKDIR /home

# Install base utilities
RUN apt-get update \
    && apt-get install -y build-essential \
    && apt-get install -y wget \
    && apt-get install -y git \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Install miniconda
ENV CONDA_DIR /opt/conda
RUN wget --quiet https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda.sh && \
    /bin/bash ~/miniconda.sh -b -p /opt/conda
# Put conda in path so we can use conda activate
ENV PATH=$CONDA_DIR/bin:$PATH

RUN conda create --name intheirownwords python=3.10
SHELL ["conda", "run", "-n", "intheirownwords", "/bin/bash", "-c"]

# install required python libs
COPY setup/ui-requirements.txt requirements.txt
RUN pip install -r requirements.txt

COPY src/es_*.py .
COPY src/llm.py .
COPY src/ui.py .

COPY src/pages/*.py ./pages

ENTRYPOINT ["conda", "run", "--no-capture-output", "-n", "intheirownwords", "streamlit", "run", "ui.py"]